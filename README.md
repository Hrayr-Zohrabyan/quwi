# quwi

## Build Setup

``` bash
# create nuxt.config.js file from file nuxt.config.example.js
$ Write your QUWI_API in the file nuxt.config.js

# install dependencies
$ npm install

# serve with hot reload at localhost:3000
$ npm run dev

# go to page http://localhost:3000/login
```
